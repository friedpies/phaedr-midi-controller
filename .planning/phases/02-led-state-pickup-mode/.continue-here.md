---
phase: 02-led-state-pickup-mode
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-02-28T04:53:01.752Z
---

<current_state>
Paused mid-session. Phase 02-03 Task 2 (hardware checkpoint) is still open. This session made significant ad-hoc changes to button/LED routing outside the formal plan. Those changes are uncommitted and build cleanly.

**Do not resume the old executor agent (a406623adb38aee22) — it is stale.**

Uncommitted changes (6 files):
- src/inputManager.cpp — restructured button assignments, readAll(), removed context-button logic
- src/inputManager.h — added _selectedTrack field
- src/mcuConfig.h — added track note base constants, removed MCU_LOOP_GRID_INDEX
- src/mcuProtocol.cpp — bypassed challenge-response validation (handshake fix)
- src/noteRegistry.cpp — added unregisterButton()
- src/noteRegistry.h — declared unregisterButton()
</current_state>

<completed_work>

## Plans Complete (prior sessions)
- **02-01**: MCUButton blink state machine. Commits: c431d1f, 3f19ddf.
- **02-02**: Fader pickup FSM (SYNCED/OUT_OF_SYNC). Commits: 1538ff4, 0d18cfb.
- **02-03 Task 1**: Wire Phase 2 integration — mcuConfig.h, pitch bend receive, updateBlinks, mode LEDs. Commit: 6c3435e.

## This session (ad-hoc, uncommitted)
- Attempted K1–K4 track-context system (mute/solo/arm per selected track) → implemented then rolled back — too complex, not yet needed
- Settled on simplified grid layout:
  - **K1–K12**: inert (noteNum=-1, LEDs available for future phases)
  - **K13**: Loop enable/disable (MCU note 86, LED_K13) — was K9 placeholder, now the real home
  - **K14/K15/K16**: Record/Stop/Play unchanged
- **P1–P8**: bidirectional SELECT — send SELECT notes 24–31 on press, receive LED feedback from Logic via NoteRegistry. _selectedTrack maintained locally.
- **P8**: labeled master track; currently SELECT Ch8 (note 31) — MCU master is ch9, to be revisited
- **Handshake bypass**: validateChallengeResponse() always returns true — was silently blocking readAll(), causing button presses to trigger ripple animation even when Logic was connected
- Added NoteRegistry::unregisterButton() — kept for potential future context-button use
</completed_work>

<remaining_work>

## Immediate: commit the session changes
```
git add src/inputManager.cpp src/inputManager.h src/mcuConfig.h src/mcuProtocol.cpp src/noteRegistry.cpp src/noteRegistry.h
git commit -m "feat: track selectors (P1-8 SELECT), K13 Loop, handshake fix, K1-12 inert"
```

## 02-03 Task 2: Hardware verification
Upload: `pio run --target upload`

1. **Handshake**: Buttons should no longer ripple when Logic is connected — readAll() should run
2. **Track selection**: Click a track in Logic → corresponding P button lights up. Press a P button → Logic selects that track.
3. **K13 Loop**: Press K13 → Logic toggles loop. Loop LED (K13) reflects state.
4. **Transport K14/K15/K16**: Record/Stop/Play still work.
5. **Pickup mode**: Fader blink still works (fader FSM untouched).
6. **Mode LEDs**: K10/K11 still receive Punch/Metro LED feedback from Logic (MCU_NOTE_PUNCH_IN=85, MCU_NOTE_METRONOME=89 — still pointing to inert buttons with working LEDs).

After hardware test → create 02-03-SUMMARY.md → run phase verifier → update roadmap.
</remaining_work>

<decisions_made>

- K1–K12 inert: will be assigned in a future phase when grid layout is finalised
- K13 = Loop: moved from K9 placeholder; direct registration, MCU_LOOP_GRID_INDEX constant removed
- P1–P8 = SELECT bidirectional (notes 24–31): Logic drives LEDs via NoteRegistry; press sends note to Logic
- P8 = master track: currently note 31 (SELECT Ch8); MCU master is technically ch9 — deferred
- Handshake bypass: DIY controller, security not needed; was blocking all button MIDI output
- unregisterButton(): added but not currently used; kept for future context-sensitive routing
- Fader pickup blink vs SELECT LED: conflict acknowledged, accepted for now
- `setHandlePitchChange` (not `setHandlePitchBend`) — Teensyduino API name (prior session decision, still correct)
</decisions_made>

<blockers>

- **P8 master track note**: SELECT Ch8 (note 31) selects track 8, not master bus. To be resolved when master track behavior is scoped.
- **K10/K11 Punch/Metro LEDs**: These grid buttons are inert (noteNum=-1) but still registered in NoteRegistry for Punch/Metro feedback. LED updates from Logic still work. Verify note numbers 85/89 are correct against Logic MIDI monitor.
</blockers>

<context>
User wanted to simplify after the K1–K4 context button plan felt wrong. Current state: transport (K13–K16) + track selection (P1–P8 SELECT) + K1–K12 as blank slates. Handshake was silently broken — buttons were in ripple-only mode even when Logic was connected. Fixed by skipping challenge-response. Hardware test will confirm everything works end-to-end.
</context>

<next_action>
1. Commit uncommitted changes (see command above)
2. Upload: `pio run --target upload`
3. Test with Logic: verify no ripple, P buttons select tracks, K13 toggles loop
4. Report results to resume plan 02-03 Task 2 checkpoint
</next_action>
